# MANAGED BY PUPPET!
# modules/couchpotato/templates/nginx_vhost.erb
# Changes WILL BE overwritten! Any permanent changes must be checked
# into git.

location /couchpotato {
	include deny.conf;
	include proxy.conf;

	proxy_pass http://localhost:<%= scope.lookupvar('couchpotato::port') %>/couchpotato;
}
